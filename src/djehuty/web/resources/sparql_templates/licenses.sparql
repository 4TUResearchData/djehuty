{% extends "prefixes.sparql" %}
{% block query %}
SELECT DISTINCT ?id ?name ?url
WHERE {
  GRAPH <{{state_graph}}> {
    ?url             rdf:type                 djht:License ;
                     col:id                   ?id ;
                     col:name                 ?name .
  }
}
{% endblock %}
