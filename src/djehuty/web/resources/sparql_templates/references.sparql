{% extends "prefixes.sparql" %}
{% block query %}
SELECT DISTINCT ?id ?url
WHERE {
  GRAPH <{{state_graph}}> {
    ?container      rdf:type/rdfs:subClassOf djht:Container .
    {%- if account_id is not none %}
    ?container      col:account_id           {{account_id}} .
    {%- endif %}

    ?item_uri       col:container            ?container .
    ?item_uri       col:references/rdf:rest* ?rest .
    ?rest           col:index                ?id .
    ?rest           rdf:first                ?url .
  }
  FILTER (?item_uri = <{{item_uri}}>)
}
ORDER BY ASC(?id)
{% endblock %}
